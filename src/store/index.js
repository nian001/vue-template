import Vue from 'vue';
import Vuex from 'vuex';
Vue.use(Vuex);

const state = {};

// getters
const getters = {};

const actions = {};

const mutations = {};

export default new Vuex.Store({
  modules: {},
  state,
  getters,
  actions,
  mutations,
});
